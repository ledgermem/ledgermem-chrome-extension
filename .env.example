# Settings are stored in chrome.storage.sync via the Options page.
# Storage keys:
#
# apiKey              — LedgerMem API key
# workspaceId         — Active workspace ID
# defaultWorkspaceId  — Fallback workspace
# hotkey              — User-customizable hotkey label (real binding lives in chrome://extensions/shortcuts)
